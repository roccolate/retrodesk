name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  RETROCORE_SPEC_COMMIT: fa027a04fca45728c2c09833388c553bfa8a5a54
  VCPKG_COMMIT: f87344cac03158cbf1467264565f1fd36b382a24

jobs:
  linux:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout RetroDesk
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Checkout pinned retrocore-spec fixtures
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: roccolate/retrocore-spec
          ref: ${{ env.RETROCORE_SPEC_COMMIT }}
          path: retrocore-spec

      - name: Install pinned toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake gcc libncurses-dev clang-tidy-18 python3
          clang-tidy-18 --version

      - name: Guard test oracles
        run: python3 scripts/check_test_oracles.py

      - name: Configure Debug and Release
        run: |
          for config in Debug Release; do
            lower=$(echo "$config" | tr '[:upper:]' '[:lower:]')
            cmake -S . -B "build/$lower" \
              -DCMAKE_BUILD_TYPE="$config" \
              -DRETROCORE_SPEC_DIR="$PWD/retrocore-spec" \
              -DRETROCORE_SPEC_REQUIRED=ON
          done

      - name: Configure sanitizer build
        run: |
          cmake -S . -B build/sanitizers \
            -DCMAKE_BUILD_TYPE=Debug \
            -DRETROCORE_SPEC_DIR="$PWD/retrocore-spec" \
            -DRETROCORE_SPEC_REQUIRED=ON \
            -DENABLE_SANITIZERS=ON \
            -DENABLE_STRICT_WARNINGS=ON \
            -DENABLE_WERROR=ON

      - name: Static analysis
        run: |
          find src -name '*.c' -print0 | xargs -0 -n1 clang-tidy-18 \
            -p build/debug \
            --checks='-*,clang-analyzer-core.*,clang-analyzer-security.*,-clang-analyzer-core.NullDereference,-clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling' \
            --warnings-as-errors='*'

      - name: Build and test Debug
        run: |
          cmake --build build/debug --parallel
          ctest --test-dir build/debug --output-on-failure

      - name: Build and test Release
        run: |
          cmake --build build/release --parallel
          ctest --test-dir build/release --output-on-failure

      - name: Build and test with sanitizers
        env:
          ASAN_OPTIONS: detect_leaks=1:halt_on_error=1:strict_string_checks=1
          UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
        run: |
          cmake --build build/sanitizers --parallel
          ctest --test-dir build/sanitizers --output-on-failure

      - name: Compare test manifests
        run: python3 scripts/compare_ctest_manifests.py build/debug build/release

      - name: Check DOS source manifest
        run: python3 scripts/check_djgpp_sources.py

      - name: Non-interactive startup smoke
        run: make smoke-ci BUILD_DIR=build/debug CONFIG=Debug STRICT=ON WERROR=ON

  windows:
    runs-on: windows-2025
    strategy:
      fail-fast: false
      matrix:
        config: [Debug, Release]
    steps:
      - name: Checkout pinned sources quietly
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          git init --quiet .
          git remote add origin "https://github.com/${{ github.repository }}.git"
          git fetch --quiet --no-tags --depth=1 origin "+${{ github.ref }}:refs/remotes/origin/ci"
          git checkout --quiet --detach refs/remotes/origin/ci
          git clone --quiet --no-checkout https://github.com/roccolate/retrocore-spec.git retrocore-spec
          git -C retrocore-spec fetch --quiet --depth=1 origin $env:RETROCORE_SPEC_COMMIT
          git -C retrocore-spec checkout --quiet --detach $env:RETROCORE_SPEC_COMMIT

      - name: Install pinned vcpkg
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          git clone --quiet https://github.com/microsoft/vcpkg.git vcpkg
          git -C vcpkg checkout --quiet --detach $env:VCPKG_COMMIT
          .\vcpkg\bootstrap-vcpkg.bat -disableMetrics *> vcpkg-bootstrap.log
          if ($LASTEXITCODE -ne 0) {
            Get-Content vcpkg-bootstrap.log -Tail 120
            exit $LASTEXITCODE
          }
          .\vcpkg\vcpkg.exe install pdcurses:x64-windows *> vcpkg-install.log
          if ($LASTEXITCODE -ne 0) {
            Get-Content vcpkg-install.log -Tail 120
            exit $LASTEXITCODE
          }
          Get-Content vcpkg-install.log -Tail 12

      - name: Configure
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          $toolchain = "$PWD\vcpkg\scripts\buildsystems\vcpkg.cmake"
          $pdcursesRoot = "$PWD\vcpkg\installed\x64-windows"
          cmake -S . -B build `
            -DCMAKE_TOOLCHAIN_FILE="$toolchain" `
            -DVCPKG_TARGET_TRIPLET=x64-windows `
            -DPDCURSES_ROOT="$pdcursesRoot" `
            -DRETROCORE_SPEC_DIR="$PWD\retrocore-spec" `
            -DRETROCORE_SPEC_REQUIRED=ON *> configure.log
          if ($LASTEXITCODE -ne 0) {
            Get-Content configure.log -Tail 160
            exit $LASTEXITCODE
          }
          Get-Content configure.log -Tail 20

      - name: Build
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          cmake --build build --config ${{ matrix.config }} --parallel *> build.log
          if ($LASTEXITCODE -ne 0) {
            Get-Content build.log -Tail 200
            exit $LASTEXITCODE
          }
          Get-Content build.log -Tail 20

      - name: Test
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          ctest --test-dir build --output-on-failure -C ${{ matrix.config }} *> test.log
          if ($LASTEXITCODE -ne 0) {
            Get-Content test.log -Tail 200
            exit $LASTEXITCODE
          }
          Get-Content test.log -Tail 40
