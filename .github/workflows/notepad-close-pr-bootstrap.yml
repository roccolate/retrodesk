name: Materialize Notepad close tests

on:
  pull_request:
    branches: [main, master]
    paths:
      - scripts/apply_notepad_close_tests.py
      - .github/workflows/notepad-close-pr-bootstrap.yml

permissions:
  contents: write

jobs:
  materialize:
    if: github.head_ref == 'agent/notepad-retrotui-port'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout source branch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: agent/notepad-retrotui-port
          fetch-depth: 0
          persist-credentials: true

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake gcc libncurses-dev python3

      - name: Materialize and validate tests
        run: |
          python3 scripts/apply_notepad_close_tests.py
          git diff --check
          python3 scripts/check_test_oracles.py
          python3 scripts/check_djgpp_sources.py
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Debug \
            -DENABLE_TESTS=ON \
            -DENABLE_STRICT_WARNINGS=ON \
            -DENABLE_WERROR=ON
          cmake --build build --parallel
          ctest --test-dir build --output-on-failure

      - name: Publish permanent test coverage
        run: |
          rm -f scripts/apply_notepad_close_tests.py
          rm -f .github/workflows/notepad-close-bootstrap.yml
          rm -f .github/workflows/notepad-close-pr-bootstrap.yml
          git config user.name "RetroDesk CI"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "test: cover Notepad close confirmation flow"
          git push origin HEAD:agent/notepad-retrotui-port
